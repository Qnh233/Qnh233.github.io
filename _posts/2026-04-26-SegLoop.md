---
title: 'SegLoop/'
date: 2026-04-26
permalink: /posts/2026/04/segloop/
tags:
  - Agent
  - LLM
  - 项目
---

SegLoop/

├── pyproject.toml              # 依赖管理

├── .env.example                # 配置模板

├── README.md                   # 快速上手文档

├── docker/Dockerfile           # mmseg 容器环境

├── scripts/run_once.py         # 手动触发脚本（Week 1 验收工具）

├── experiments/v0.0/README.md  # 基线占位文档

├── segloop/

│   ├── core/

│   │   ├── config.py           # pydantic-settings 全局配置

│   │   ├── state.py            # GlobalStatePool + SegLoopGraphState

│   │   ├── llm.py              # LLM 封装（Jinja2 模板 + token 统计）

│   │   └── graph.py            # LangGraph 主图（10 个节点 + 条件路由）

│   ├── agents/

│   │   ├── master.py           # 迭代假设生成（含降级规则策略）

│   │   ├── train_agent.py      # Config 生成 + 训练启动 + OOM 自修复

│   │   ├── eval_agent.py       # 指标解析 + 按需可视化 + README 生成

│   │   ├── debug_agent.py      # 3 次重试硬上限 + 错误分类 + 人工升级

│   │   ├── data_agent.py       # 数据集验证

│   │   └── research_agent.py   # 三级检索 + 适配性/难度评估

│   ├── tools/

│   │   ├── mmseg_tools.py      # config 校验、train_launch、OOM 降 batch

│   │   ├── metric_tools.py     # mmengine 日志解析、vis_gen、消融表格

│   │   ├── version_tools.py    # 版本快照、config diff、git commit、README

│   │   ├── search_tools.py     # PWC API + Semantic Scholar + arXiv

│   │   └── experience_tools.py # pre_round_check + post_round_update

│   ├── memory/experience_db.py # ChromaDB 双轨经验库（failure + success）

│   ├── prompts/                # Jinja2 提示词模板（master/train_config/eval_conclusion）

│   ├── ui/review_cli.py        # 复盘卡 CLI + interrupt 处理

│   └── main.py                 # Click CLI（run / review / status）

└── tests/                      # 6 个测试文件，53 passed / 7 skipped

#### 核心验收结果

#### 下一步操作（使用前必做）



